# NewsLens ingestion + NLP pipeline
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.2
scikit-learn>=1.5      # TF-IDF + cosine similarity for clustering
numpy>=1.26
python-dotenv>=1.0
openai>=1.40           # summaries + bias assist (optional in mock mode)
psycopg[binary]>=3.2   # Postgres writes (live mode)
